requests>=2.31.0
python-dotenv>=1.0.0
colorama>=0.4.6
qrcode[pil]>=7.4.2

# 渠道适配器
python-telegram-bot>=20.7
lark-oapi>=1.3.0          # 飞书官方 SDK（WebSocket 长连接，无需公网IP）

# 数据源
# akshare 已移除，所有功能通过腾讯财经/东方财富/Binance 直接 HTTP 获取

# 任务调度（替代 schedule + time.sleep）
APScheduler>=3.10.0